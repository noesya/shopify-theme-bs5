{% layout 'theme.fluid' %}

<div class="container py-5">
  <div class="row">
    <div class="col-md-6 offset-md-3">
      <h1>{{ page.title }}</h1>
      <div>
        {{ page.content }}
        {% include "form-contact" %}
      </div>
    </div>
  </div>
</div>
