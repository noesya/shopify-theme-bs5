<!doctype html>
<html lang="{{ shop.locale }}">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  {% include 'header-assets' %}
  {% include 'header-seo' %}
  {{ content_for_header }}
</head>
<body class="{{ page_title | handle }} {{ template | replace: '.', ' ' | truncatewords: 1, '' }}">
  {%- section 'top' -%}
  {%- section 'header' -%}
  {%- include 'breadcrumbs' -%}
  <main>
    <div class="container py-5">
      {{ content_for_layout }}
    </div>
  </main>
  {%- section 'footer' -%}
  {% include 'footer-assets' %}
</body>
</html>
