{%- unless alt -%}
  {% assign alt = src.alt %}
{%- endunless -%}

{%- unless size -%}
  {% assign size = 'large' %}
{%- endunless -%}

<img
  {%- if crop -%}
    src="{{ src | img_url: size, crop: crop }}"
  {%- else -%}
    src="{{ src | img_url: size }}"
  {%- endif -%}
  alt="{{ alt | escape }}"
  class="{{ class }}"
  {%- if src.height -%}
    height="{{src.height}}"
  {%- endif -%}
  {%- if src.width -%}
    width="{{src.width}}"
  {%- endif -%}
  loading="lazy"
  >
