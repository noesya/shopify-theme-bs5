{%- assign loading = 'loading="lazy"' -%}
{%- if lazy == false -%}
  {%- assign loading = '' -%}
{%- endif -%}
<img
  src="{{ src | img_url: size }}"
  alt="{{ alt | escape }}"
  class="{{ class }}"
  {{ loading }}>
