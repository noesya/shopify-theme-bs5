<ul class="navbar-nav ml-auto mb-2 mb-lg-0">
  <li class="nav-item">
    <a href="{{ routes.search_url }}" class="nav-link" title="{{ 'general.search.title' | t }}">
      <span class="material-icons-outlined">search</span>
    </a>
  </li>
  <li class="nav-item">
    <a href="{{ routes.cart_url }}" class="nav-link cart__notif" title="{{ 'cart.general.title' | t }}">
      <span class="material-icons-outlined">shopping_bag</span>
      <span class="notif js-cart-notif">{{ itemCart }}</span>
    </a>
  </li>
  {% if shop.customer_accounts_enabled %}
    {% if customer %}
      <li class="nav-item">
        <a href="{{ routes.account_url }}" class="nav-link" title="{{ 'customer.account.title' | t }}">
          <span class="material-icons-outlined">person_outline</span>
        </a>
      </li>
    {% else %}
      <li class="nav-item">
        <a href="{{ routes.account_login_url }}" class="nav-link" title="{{ 'customer.account.title' | t }}">
          <span class="material-icons-outlined">person_outline</span>
        </a>
      </li>
    {% endif %}
  {% endif %}
</ul>
